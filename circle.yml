dependencies:
  override:
    - pyenv local 2.7.5 3.4.0 3.5.0
  pre:
    - pip install -r dev_requirements.txt

deployment:
production:
  branch: [master, develop]
  commands:
    - ./merge_to_developer-neustar-iot.sh

# notify:
#   webhooks:
#     # - url: https://example.com/hooks/circle
#     - url: https://circleci.com/gh/OneID/developer-neustar-iot

test:
  pre:
    - flake8
  override:
    - tox
